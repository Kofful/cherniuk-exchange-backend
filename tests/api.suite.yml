actor: ApiTester
modules:
  enabled:
    - Symfony:
        app_path: 'src'
        part: services
        environment: "testing"
    - REST:
        depends: Symfony
        part: Json

step_decorators:
  - \Codeception\Step\AsJson
